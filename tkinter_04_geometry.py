import tkinter as tk

root = tk.Tk()
root.title("TKinter")
root.resizable(width=True, height=False)   # --------> Podemos cambiar su anchura, pero no su altura. también se puede usar (True, False)

root.mainloop()